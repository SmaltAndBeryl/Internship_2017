login:
 selectSql: > 
   select applicationId,userRole,userStatus 
    from user inner join application where user.userId=:userId and user.password=:password
    
 checkUserSql: >
   SELECT EXISTS(SELECT * FROM user WHERE userId=:userId and password=:password)